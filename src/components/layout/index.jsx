import React from 'react';
import Footer from '../footer';
import Header from '../header';

const LayOut = () => {
    return (
        <React.Fragment>
            <Header />
            <div>레이아웃입니다.</div>
            <Footer />
        </React.Fragment>
    );
};

export default LayOut;
